.element
  %caption Name
  = f.text_field :name
.element
  %caption Starts At (UTC)
  = f.text_field :utc_starts_at
.element
  %caption Ends At (UTC)
  = f.text_field :utc_ends_at
.element
  %caption Timezone
  = time_zone_select(@semester, :timezone, ::ActiveSupport::TimeZone.us_zones, :default => 'America/Chicago')
