%h2.title
  = link_to 'Rubrics', { :controller => 'rubrics' }
  &gt;
  = link_to @rubric.name, @rubric
  &gt;
  Edit Rubric

- form_for @rubric, :url => rubric_path(@rubric) do |f|
  = render :partial => 'form', :locals => { :f => f }
  = submit_tag 'Update Rubric'
